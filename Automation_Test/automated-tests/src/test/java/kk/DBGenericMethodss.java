//package kk;


/*public class DBGenericMethodss {

}
    public class PruebaArrayListSerializable {

        protected static ArrayList<Object> badeDatos = new ArrayList<Str>();

        public static void main(String[] args) throws SQLException {
            PostgreSQL sql = new PostgreSQL(); ResultSet rs = null;
            //Object[][] obj = null;
            sql.add(" ", " ", " ");
            String sql = "SELECT * FROM ";
            rs = mysql.consultar(sql);
            ResultSetMetaData rsmd = null;
            rsmd = rs.getMetaData();
            int columnas = rsmd.getColumnCount();
            while(rs.next()){
                HashMap row = new HashMap();
                badeDatos.add(row);
                for(int i=1; i<=columnas;i++){
                    row.put(rsmd.getColumnName(i), rsmd.getColumnCount()); }
            } for(Object o : badeDatos) {
                    System.out.println(o.toString()); }


}*/
