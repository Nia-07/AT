package Scenarios;

public class DataBaseRun {
/*
    public static void main (String[] args) throws SQLException {

        List<String> codeList = new ArrayList<String>();
        String consult = "SELECT code FROM \"general\".acceptance_of_conditions WHERE email = 'tania.rocha@santanderconsumer.co' AND generated_date BETWEEN '2022-07-26' AND NOW() AND acceptance_date IS null order by generated_date desc fetch first 1 rows only";
        String column = "code";
        DataBaseMethods.consultDB(consult,column, codeList);

        System.out.println("El tamano de la lista es: "+codeList.size());
        System.out.println(codeList);

        for(int index=0; index<codeList.size(); index++){
            String valor = codeList.get(index);
            System.out.println(valor);
            /*if(codeList.contains("perro")){
                codeList.remove(codeList.indexOf("perro"));
            }
        }

        }




*/
    }











